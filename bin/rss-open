#!/bin/bash

if printf "$1" | grep -q "youtube.com\|youtu.be\|mp4\|mkv\|webm"; then
	echo "$(date)	$1" >> $HOME/OneDrive/videos.txt
	firejail mpv "$1"
else
	echo "$(date)	$1" >> $HOME/OneDrive/articles.txt
	firejail firefox "$1"
fi
